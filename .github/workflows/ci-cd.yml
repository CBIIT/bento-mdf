name: ci-cd

on: [push, pull_request]

jobs:
  test-build:
    permissions:
      content: read
      packages: read
    uses: CBIIT/bento-workflows/.github/workflows/test-build.yml@v3.0.26
    with:
      workdir: ./python
      versions: |
        ["3.10","3.11","3.12","3.13"]
      no-test-mdb: true
    secrets: inherit
